import 'package:flutter/material.dart';

class SCSGridView extends StatelessWidget {
  const SCSGridView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("SGridview"),),
    );
  }
}
